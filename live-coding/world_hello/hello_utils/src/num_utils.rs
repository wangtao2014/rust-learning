
pub fn num_add(left: usize, right: usize) -> usize {
    left + right
}

pub fn num_sub(left: usize, right: usize) -> usize {
    left - right
}

pub fn num_mul(left: usize, right: usize) -> usize {
    left * right
}

pub fn num_div(left: usize, right: usize) -> usize {
    left / right
}

